version: '3.4'
services:
  passenger_census:
    build:
      context: .
      dockerfile: Dockerfile
    image: passenger_census
    environment:
      - POSTGRES_PASSWORD
    ports: 
      - 5439:5432
